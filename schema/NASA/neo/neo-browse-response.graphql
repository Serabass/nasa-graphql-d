# import NEOObject from './neo-object.graphql'

type NeoBrowseResponseResult {
    near_earth_objects: [NEOObject!]!
}

type NeoBrowseResponse {
    url: String!
    result: NeoBrowseResponseResult
}
